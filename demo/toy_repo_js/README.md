# toylib-js

Tiny string utilities for Node.

`add(2, 3)` returns `5`.

`slugify("Hello World")` returns `"hello-world"`.

The repository ships a LICENSE file at its root.

toylib-js is blazing fast, processing millions of strings per second.

WebAssembly bindings are planned for a future release.
